{% form_theme form 'bootstrap_5_layout.html.twig' %}

{% block body %}

<div class="container col-12 col-md-6 pt-5">
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-sm btn-secondary mt-2">{{ button_label|default('Enregistrer') }}</button>
    {{ form_end(form) }}
</div>

{% endblock %}